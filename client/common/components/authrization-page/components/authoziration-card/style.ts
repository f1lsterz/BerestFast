import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    width: "80%",
    height: "60%",
    backgroundColor: "#1270DB",
    borderRadius: 20,
    alignItems: "center",
  },
  innerContainer: {
    justifyContent: "center",
    alignItems: "center",
  },
});

export default style;

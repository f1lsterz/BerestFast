import React from "react";
import MainContainer from "@client/common/components/main-container";
import SingUpAuthPassword from "@client/common/password-pages/sign-up-password/sign-up-password";

const SignUpPassword = () => {
  return (
    <MainContainer>
      <SingUpAuthPassword/>
    </MainContainer>
  );
};

export default SignUpPassword;

import React from "react";
import MainContainer from "@client/common/components/main-container";
import SingInAuthPassword from "@client/common/password-pages/sign-in-password";

const SignInPassword = () => {
  return (
    <MainContainer>
      <SingInAuthPassword />
    </MainContainer>
  );
};

export default SignInPassword;
